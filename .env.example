# ==========================================
# JunLite CRM V6.0 环境变量配置示例
# ==========================================

# ------------------------------------------
# 数据库配置 (MySQL)
# ------------------------------------------
MYSQL_ROOT_PASSWORD=crm_root_password_2026
MYSQL_DATABASE=crm_db
MYSQL_USER=crm_user
MYSQL_PASSWORD=crm_password_2026

# ------------------------------------------
# JWT认证配置
# ------------------------------------------
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=7d

# ------------------------------------------
# CORS跨域配置
# ------------------------------------------
# 开发环境
CORS_ORIGIN=http://localhost:80,http://localhost:5173,http://localhost:8080,http://localhost:8081,http://127.0.0.1:5173

# 生产环境示例（请根据实际域名修改）
# CORS_ORIGIN=https://admin.yourdomain.com,https://h5.yourdomain.com

# ------------------------------------------
# 服务器配置
# ------------------------------------------
NODE_ENV=production
PORT=3001

# ------------------------------------------
# 文件上传配置
# ------------------------------------------
UPLOAD_DIR=/app/uploads
MAX_FILE_SIZE=10485760

# ------------------------------------------
# 数据库连接配置（后端使用）
# ------------------------------------------
DB_HOST=mysql
DB_PORT=3306
DB_USERNAME=crm_user
DB_PASSWORD=crm_password_2026
DB_DATABASE=crm_db
