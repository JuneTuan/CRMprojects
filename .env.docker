# Docker环境配置文件

# MySQL数据库配置
MYSQL_ROOT_PASSWORD=crm_root_password_change_in_production
MYSQL_DATABASE=crm_db
MYSQL_USER=crm_user
MYSQL_PASSWORD=crm_password_change_in_production

# JWT配置
JWT_SECRET=your-jwt-secret-key-change-in-production

# CORS配置
CORS_ORIGIN=http://localhost:8080,http://localhost:8081